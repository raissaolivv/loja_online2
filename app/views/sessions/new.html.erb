<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container d-flex align-items-center justify-content-center min-vh-100">
        <div class="card shadow p-4" style="max-width: 400px; width: 100%;">
            <h1 class="text-center mb-4">Login</h1>

            <% if flash[:error] %>
                <p class="alert alert-danger"><%= flash[:error] %></p>
            <% end %>

            <% if flash[:notice] %>
                <p class="alert alert-success"><%= flash[:notice] %></p>
            <% end %>

            <%= form_for :homepage, url: home_path do |f| %>
                <!-- CSRF Token -->
                <%= csrf_meta_tags %>

                <!-- Username Field -->
                <div class="mb-3">
                    <%= label_tag :username, "Nome de Usuário", class: "form-label" %>
                    <%= text_field_tag :username, nil, class: "form-control", placeholder: "Digite seu usuário", required: true %>
                </div>
                
                <!-- Password Field -->
                <div class="mb-3">
                    <%= label_tag :password, "Senha", class: "form-label" %>
                    <%= password_field_tag :password, nil, class: "form-control", placeholder: "Digite sua senha", required: true %>
                </div>

                <!-- Submit Button -->
                <div class="d-grid">
                    <%= submit_tag "Entrar", class: "btn btn-primary" %>
                </div>
            <% end %>

            <div class="mt-3 text-center">
                <small>Não tem uma conta? <%= link_to "Cadastre-se", cadastro_path %></small>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
